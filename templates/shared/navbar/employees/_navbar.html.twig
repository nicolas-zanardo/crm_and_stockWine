<nav class="navbar navbar-expand-md navbar-dark bg-dark" id="navbar-employees">

        <div class="d-flex align-items-center block-employees-nav-bar">
            <a class="logo-navbar-employees" href="{{ path('home_page') }}">
                <img src="{{ asset('build/white_logo.png') }}" alt="les vielles vignes"></a>
            <div class="navbar-brand"><i class="fas"></i> Menu</div>

            <div class="container fluid d-flex employees">
                <div>{{ title_page }}</div>
            </div>
            <div class="d-flex login">
                <a href=""><i class="fas fa-user"></i></a>
                <a href=""><i class="fas fa-sign-out-alt"></i></a>
            </div>
        </div>
</nav>